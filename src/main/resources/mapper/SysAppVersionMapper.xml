<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xhn.sys.mapper.SysAppVersionMapper">

    <select id="getLastVersion" resultType="com.xhn.sys.model.SysAppVersionDTO">
        SELECT
            id,
            platform,
            version,
            build_number,
            is_force_update,
            download_url,
            changelog,
            release_date,
            created_time ,
            updated_time
        FROM sys_app_version
        WHERE platform = 'android'  -- 或 'ios'
        ORDER BY build_number DESC
            LIMIT 1
    </select>
</mapper>
